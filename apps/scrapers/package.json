{
  "name": "scrapers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json && wrangler deploy --dry-run",
    "cf-typegen": "wrangler types",
    "start:node": "bun src/node/main.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250617.0",
    "@types/bun": "^1.2.16",
    "typescript": "5.8.3",
    "wrangler": "4.21.x"
  }
}
