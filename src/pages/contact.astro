---
import Layout from "../layouts/Layout.astro";
import { siteConfig } from "@/config/site";
import ContactFormReact from "@/components/ContactFormReact";
---

<Layout title={`Contact Us | ${siteConfig.name}`} description="Get in touch with our team">
  <ContactFormReact client:load siteConfig={siteConfig} />
</Layout>
